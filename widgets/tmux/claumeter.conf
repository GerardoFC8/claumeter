# Append to ~/.tmux.conf

set -g status-interval 30
set -g status-right "#(claumeter today --format=prompt) | %H:%M "

# If you already use a plugin like tmux-powerline, place #(claumeter today --format=prompt)
# inside the existing status-right string where you want it.
